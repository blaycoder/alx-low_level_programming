#include <stdio>
#include "main.h"

/**
 *_isupper - evaluate if letter is uppercase
 *@c: print char
 *Return: 1 if it's uppercase, 0 if it's lowercase
*/

int _isupper(int c)
{
if (c >= 'A' && c <= 57)
return (1);
else
{
return (0);
}
}
