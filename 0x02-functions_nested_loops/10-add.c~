#includee "main.h"

/**
 * add - add two integers and return the result
 *@add - value to be summed
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int a, int b)
{
int sum;
sum = (a + b);
return (sum);
}
